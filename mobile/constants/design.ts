/**
 * Токены из макета dashboard-design.html.
 *
 * Палитра фиксированная и светлая: дашборд не наследует тему устройства —
 * цифры должны читаться одинаково у всех, кто открыл один и тот же отчёт.
 */

export const Palette = {
  /** Основной текст. Он же фон чёрных элементов — логотипа и кнопки. */
  ink: '#101317',
  /** Поля ввода и карточки. */
  paper: '#FFFFFF',
  /** Заливка неактивных сегментов и переключателей. */
  field: '#EDF0F4',
  /** Фон экрана. */
  appBg: '#F4F6F9',
  /** Рамки и разделители. */
  line: '#DCE2EA',
  /** Вторичный текст: подписи, подсказки, лейблы полей. */
  muted: '#6C7683',
  /** Третичный текст: подсказки в пустых полях ввода. */
  dim: '#98A2AF',
  /** Ошибки: неверный пароль, недоступный сервер. */
  warn: '#C1440E',
  /** Рост показателя. */
  ok: '#0F7B57',
} as const;

export const Radius = {
  /** Поля ввода и кнопки. */
  control: 11,
  /** Логотип. */
  mark: 13,
  /** Карточки. */
  card: 14,
} as const;

/**
 * Фирменные цвета маркетплейсов, парами для градиента: от тёмного к яркому.
 *
 * Цвет здесь не украшение, а признак: по нему видно, чьи данные на экране.
 * Поэтому он живёт в токенах, а не в стилях конкретного экрана.
 */
export const Brand = {
  wb: {
    /** Полосы, иконки разделов, активная вкладка. */
    accent: '#CB11AB',
    /** Карточка выбора: от тёмного к яркому. */
    gradient: ['#7A0A80', '#CB11AB'],
  },
} as const;
